import { sleeve as librarySleeve } from '@freesewing/library'

export const sleeve = {
  from: librarySleeve,
  name: '{{ name }}.sleeve',
  hide: { from: true },
  measurements: [],
  options: {},
  draft: ({ part }) => {
    return part
  },
}
