import { cuff as bibiCuff } from '@freesewing/bibi'

export const cuff = {
  from: bibiCuff,
  name: '{{ name }}.cuff',
  hide: { from: true },
  options: {},
  draft: ({ part }) => {
    return part
  },
}
